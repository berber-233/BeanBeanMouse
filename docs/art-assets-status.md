# 美术资源状态（临时占位标注）

> 状态更新：2026-08-16。用户审阅后判定当前矢量吉祥物与运输动画为**临时占位作品（质量未达标）**，
> 仅用于保证网站功能可运行，**不代表最终视觉**。后续将由用户提供更专业的模型/渠道重新制作，
> 替换后同文件名覆盖即可（代码无需改动）。

## 待替换资产清单

| 资产 | 用途 | 当前状态 | 替换方式 |
| --- | --- | --- | --- |
| `assets/mascot-vector.svg` | 导航 Logo / 帮助按钮 / 镖局头像 | 临时占位 | 新图出稿后覆盖同名文件 |
| `assets/mascot-vector.png` | OG 分享图 / 浏览器图标 | 临时占位（由 SVG 导出） | 随 SVG 一并替换 |
| `assets/tip-hamster-empty.svg` | 打赏空碗 | 临时占位 | 同上 |
| `assets/tip-hamster-full.svg` | 打赏金币碗 | 临时占位 | 同上 |
| `assets/transport-land-anim.svg` | 陆运实时运输动画 | 临时占位 | 同上 |
| `assets/transport-sea-anim.svg` | 海运实时运输动画 | 临时占位 | 同上 |
| `assets/transport-air-anim.svg` | 空运实时运输动画 | 临时占位 | 同上 |

## 替换要求（给后续制作方）

- 角色：金丝熊仓鼠「豆豆鼠」，红围巾，黄白暖色系，与网站主题一致；角色本体不变，仅装饰可变。
- 运输动画：Flash 风格小动画，三阶段「出发 → 运输中 → 到达」，每幕约 12 秒循环；
  陆运 = 马 + 马车、海运 = 三桅帆船、空运 = 空艇；SVG（SMIL）或等效可嵌入 `<img>` 的矢量动画格式。
- 替换后保持文件名不变（代码零改动），并跑一遍 `node test/verify.cjs` 回归确认。
